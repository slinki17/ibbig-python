Repository for Master's Thesis Project

Implementation and development of the iBBiG R package in Python

The first task was to convert the algorithm from R to Python while keeping the core C code intact.
Then, the algorithm was developed further to work with more than 2 dimensions.
This extended the original 2D biclustering problem into 3D and 4D multi-block clustering.
